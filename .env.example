# Required by the Slack platform.
SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Optional. Channel name (without the leading #) to send structured logs to.
# When unset, the default logger writes only to stdout/stderr.
SLACK_LOG_CHANNEL=
